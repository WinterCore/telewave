{
  "name": "telewave-ui-design",
  "private": true,
  "version": "1.0.0",
  "description": "Standalone Tailwind CSS prototypes for Telewave",
  "scripts": {
    "build": "tailwindcss -i ./src/tailwind.css -o ./styles.css --minify",
    "watch": "tailwindcss -i ./src/tailwind.css -o ./styles.css --watch"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
